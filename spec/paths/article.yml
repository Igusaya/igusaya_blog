post:
  summary: 記事投稿 
  description: 記事投稿
  tags:
    - article
  operationId: post_article
# security: TODO
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../components/schemas/article/create_request.yml'
  responses:
    200:
      description: successful post article response